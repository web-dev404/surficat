import EnterBlock from "@/common/Enter/EnterBlock";
import Authorization from "@/layout/Authorization";
const Registration = () => {
    return (
        <Authorization title={'Вход'}>
            <EnterBlock></EnterBlock>
        </Authorization>
    )
}
export default Registration
